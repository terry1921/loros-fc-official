import type {ReactNode} from 'react';

export const metadata = {
  title: 'Directiva',
  description: 'Conoce al equipo directivo de Loros Fútbol Club.',
};

export default function DirectiveLayout({children}: {children: ReactNode}) {
  return children;
}
